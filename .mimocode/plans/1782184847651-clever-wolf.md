# 修复表单编辑时同步轮询重建导致图片消失

## 问题
同步轮询触发 refreshAll() → loadQuestions() 重建题目列表，不影响表单但会触发其他副作用。

## 修复
添加 `isFormDirty` 标志，当用户在表单中选择了图片或正在编辑时，refreshAll 跳过 loadQuestions。

## 修改文件
- `www/index.html`：refreshAll 函数、图片选择/清除时设置标志
