# 本地题库项目：产品设计与工程标准宣言 (V1.0)

> **目标**：打造一个不仅功能强大，而且在交互上“诚实、透明、优雅”的 AI 增强型本地应用。

## 1. 核心设计原则 (The Core Principles)

### 1.1 系统状态的可见性 (Visibility of System Status)
- **永远不让用户等待超过 1 秒的黑盒操作**。
- **进度反馈**：所有涉及 IO（文件读写、下载）或计算（AI 推理）的长耗时操作，必须配备进度条、加载动画或百分比提示。
- **状态徽章**：关键模块（如 AI 引擎、同步状态）必须有明确的视觉徽章（已就绪、处理中、错误、未连接）。

### 1.2 防错与容错 (Error Prevention & Recovery)
- **按钮状态锁定**：在任务执行期间，必须通过 `disabled` 属性锁定触发按钮，防止用户重复触发导致的资源冲突。
- **优雅退避**：当原生 AI 引擎不可用时，系统必须自动切换到 Mock（模拟）模式或提供清晰的错误排查指南，而不是直接崩溃或报错。

### 1.3 极简主义与层级设计 (Minimalism & Visual Hierarchy)
- **重要信息优先**：核心业务卡片（如题目列表）应占据视觉重心，辅助管理功能（如 AI 状态管理）应使用卡片层级进行区隔。
- **视觉一致性**：统一使用 Gemma 4 品牌色系 (#8b5cf6 紫色) 作为 AI 功能的标志性配色。

---

## 2. 代码实现标准 (Engineering Standards)

### 2.1 异步处理规范
- **非阻塞主线程**：所有的 AI 分析、模型下载必须在异步线程或 Web Worker 中运行，绝不允许阻塞 UI 渲染。
- **事件驱动 UI**：优先使用 `CustomEvent` 或原生插件监听器 (`notifyListeners`) 来驱动 UI 刷新，实现数据与表现的解耦。

### 2.2 数据持久化原则
- **隐私至上**：除了显式开启的“远程同步”，所有 AI 提取的元数据（摘要、标签）必须默认存储在本地 `localForage` 中。
- **Schema 扩展**：在修改数据库结构时，必须编写 `_normalizeRecord` 辅助方法，确保旧数据能平滑迁移。

### 2.3 仿真与调试
- **双端自愈逻辑**：编写插件时，必须包含 `web` 平台的 Mock 实现，确保项目在开发环境（浏览器）下依然能够进行逻辑全链路测试。

---

## 3. 长期记忆执行准则
1. **每次功能迭代前**：必须对照此文档检查：是否有加载反馈？是否有按钮保护？配色是否一致？
2. **每次代码审查时**：若发现违反“系统状态可见性”的操作，必须作为最高优先级的 Bug 进行修复。
3. **文档演进**：此文档随项目成长而更新，但核心原则“用户体验不妥协”永远保持。
